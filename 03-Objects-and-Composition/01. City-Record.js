function printRecord(name, population, treasure) {
  const record = {
    name: name,
    population: population,
    treasury: treasure,
  };

  return record;
}

printRecord("Tortuga", 7000, 15000);
